

public class Kare extends Dortgen{
    Nokta nokta1 = new Nokta(getX1(), getY1());
    Nokta nokta2 = new Nokta(getX2(), getY2());
    Nokta nokta3 = new Nokta(getX3(), getY3());
    Nokta nokta4 = new Nokta(getX4(), getY4());

    public Kare(int x1, int y1, int x2, int y2, int x3, int y3, int x4, int y4) {
        super(x1, y1, x2, y2, x3, y3, x4, y4);
    }
       
    
}
